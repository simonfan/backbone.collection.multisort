define(['backbone.collection.multisort'], function (BackboneCollectionMultisort) {

    var msg = 'Main demo of the backbone.collection.multisort module running!';

	console.log(msg);

	alert(msg);
});
